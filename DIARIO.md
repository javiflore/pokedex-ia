# Diario del Proyecto

## 22/01/2026
1. Generación de archivo OpenAPI para documentar la API de PokeAPI
2. Guardado de especificación OpenAPI en formato JSON (`context/pokeapi-openapi.json`)

## 11/01/2026
1. Creación de archivo de instrucciones para el agente (`AGENT.md`)
2. Instalación de Next.js (`npx create-next-app@latest .`)
3. Migración a pnpm - más rápido, menos espacio en disco, mejor gestión de dependencias (`pnpm install`)
4. Levantar el proyecto en local (`pnpm dev`)
5. Actualización de `AGENT.md` con buenas prácticas de programación
